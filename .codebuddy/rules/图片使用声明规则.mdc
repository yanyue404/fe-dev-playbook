---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-08-03T03:01:27.774Z
provider: 
---

## 图片使用声明规则

### 触发条件
当本轮输入中包含图片文件（`@image:...`、用户上传的图片、截图工具发送的截图、设计稿等），或当前对话历史中曾出现过图片文件时，必须执行以下流程。

### 核心原则：图片声明必须在回复最开头

**在回答正文之前，必须先输出「图片使用情况」区块，让用户第一时间确认图片已被识别和处理。不要放到末尾。**

### 图片引用方式分类
| 方式 | 特征 | 处理策略 |
|------|------|---------|
| @image引用 | `@image:文件名.hash.ext`（如 `@image:7zhe.76625ab448.png`） | **必须主动获取文件路径并读取内容** |
| 对话内嵌图片 | 用户通过截图工具直接发送 | 系统已自动注入图片内容，可直接分析 |
| 路径引用 | 用户直接提供完整路径（如 `C:\xxx\image.png`） | 直接用 `read_file` 读取 |

### 处理流程

1. **识别图片并立即声明（回复最开头）**
   - 确认图片的引用方式、文件名、数量及内容类型。
   - 判断属于上述三种引用方式中的哪一种，决定后续获取策略。
   - **立即在回复最开头输出「图片使用情况」区块**，让用户确认图片已被注意到。

2. **获取图片文件（关键步骤！对 @image 引用必须执行）**
   - **@image引用的图片**：实际存储在 `%TEMP%` 目录（Windows: `C:\Users\Administrator\AppData\Local\Temp\`），文件名格式为 `原名.hash8位.扩展名`。使用 `search_file` 或 `execute_command(dir)` 在 `%TEMP%` 中搜索文件名关键部分，获取完整路径。
   - **获取到路径后**：必须使用 `read_file` 工具读取图片文件，查看实际图片内容。
   - **对话内嵌图片**：内容已在系统中可直接分析，无需额外获取。
   - **路径引用图片**：直接用 `read_file` 读取指定路径。
   - **如果搜索不到 @image 文件**：明确告知用户无法定位图片，并请求提供完整路径。

3. **分析图片并生成结论**
   - 结合图片内容与用户文字请求，给出对应的分析、还原、修复或实现方案。
   - 设计稿 → 说明做了哪些样式/布局还原；报错截图 → 说明定位到的问题及修复方案。
   - **必须基于实际查看的图片内容**，而非仅凭文件名推测。
   - 如果图片内容无法解析，需明确说明："无法解析图片内容，以下回复未基于图片信息"。

4. **图片文件的实际使用**
   - 插入文档任务 → 直接使用图片完整路径作为数据传入脚本。
   - 修改代码任务 → 根据图片中的视觉信息精准定位修改区域。

### 回复结构模板

```
## 图片使用情况

- **是否使用图片**：是 / 否
- **图片来源**：本轮上传 / 截图内嵌 / 路径引用 / 历史思维链 / 未使用
- **图片获取方式**：@image搜索%TEMP% / read_file直接读取 / 系统内嵌自动获取 / 未获取
- **使用说明**：……

---（以下是正常回答正文）---
```

### 示例

**场景：@image引用 + 设计稿还原**

```
## 图片使用情况

- **是否使用图片**：是
- **图片来源**：本轮上传
- **图片获取方式**：@image搜索%TEMP%
- **使用说明**：在 %TEMP% 中搜索到 `7zhe.76625ab448.png`，read_file 查看后识别为产品七折优惠标签设计稿，后续将据此还原折扣气泡样式。

接下来基于该设计稿还原样式……（正文内容）
```

**场景：截图内嵌 + 报错修复**

```
## 图片使用情况

- **是否使用图片**：是
- **图片来源**：截图内嵌
- **图片获取方式**：系统内嵌自动获取
- **使用说明**：截图显示 `service/order.ts` 第42行类型不匹配报错，后续将定位并修复。

定位到问题所在……（正文内容）
```

**场景：无图片**

```
## 图片使用情况

- **是否使用图片**：否
- **图片来源**：未使用
- **图片获取方式**：未获取
- **使用说明**：本轮无图片输入，回复仅基于文字描述和代码上下文生成。

……（正文内容）
```
```

关键改动：
- **核心原则**加粗强调：声明必须在回复最开头
- **步骤1合并识别+声明**：识别图片后立即在开头输出声明
- **回复结构模板**：明确声明在前、正文在后
- **所有示例**都改为声明在开头、正文紧跟其后的格式