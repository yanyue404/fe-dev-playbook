---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-08-06T02:09:20.999Z
provider: 
---

## 触发条件

当用户主动问询 token 消耗情况时（如"token 消耗"、"token 用了多少"、"消耗分析"等），在回复末尾附加「Token 消耗计量」区块。默认情况下不附加。

## 计量维度

### 输入侧（Input Tokens）

| 环节 | 标识 | 说明 |
|------|------|------|
| 系统提示词 | `sys_prompt` | 系统级指令、角色定义、工具定义等基础 prompt |
| 用户规则 | `user_rules` | always_applied_user_rules 中加载的规则（如 Karpathy 准则、图片声明规则、本规则等） |
| 记忆/上下文 | `memories` | 从 knowledge base 注入的 memories |
| 项目上下文 | `project_ctx` | git_status、project_layout、open_files、linter 等 IDE 注入信息 |
| 对话历史 | `conv_history` | 当前会话中所有之前的轮次（含工具调用和结果） |
| 用户消息 | `user_msg` | 本轮用户输入的原始消息 |
| 附加资源 | `attachments` | 用户上传的图片、文件夹、文件引用等 |
| 工具结果 | `tool_results` | 本轮工具调用返回的结果（read_file、search_content 等），需拆分展示 |

### 输出侧（Output Tokens）

| 环节 | 标识 | 说明 |
|------|------|------|
| 深度思维链 | `thinking` | 内部推理/规划过程消耗的 token |
| 文本回复 | `text_output` | 最终输出给用户的可见文本 |
| 工具调用 | `tool_calls` | 构造的函数调用参数（function name + arguments） |

## 输出格式

在回复末尾使用以下模板（单位：**k**，保留 1 位小数）：

```markdown
---
## Token 消耗计量

### 输入侧

| 环节 | token | 具体内容 |
|------|------:|---------|
| 系统提示词 (`sys_prompt`) | ~Nk | 角色定义+工具schema+content_policy+… |
| 用户规则 (`user_rules`) | ~Nk | ① Karpathy准则 ② 图片声明规则 ③ … |
| 记忆/上下文 (`memories`) | ~Nk | ① 项目架构手册(~Nk) ② Mermaid规则(~Nk) ③ … |
| 项目上下文 (`project_ctx`) | ~Nk | git_status+project_layout |
| 对话历史 (`conv_history`) | ~Nk | 之前N轮对话（含工具调用和结果） |
| 用户消息 (`user_msg`) | ~Nk | 用户原始输入摘要 |
| 附加资源 (`attachments`) | 0 | 无图片/附件 |
| 工具结果 (`tool_results`) | ~Nk | 详见下方拆解 |
| **输入小计** | **~Nk** | |

### 工具结果拆解

| 工具调用 | 返回内容 | 估算 token |
|---------|---------|-----------:|
| `read_file` xxx.vue | 组件完整源码（template+script+style） | ~Nk |
| `search_content` pattern | N个匹配结果 | ~Nk |
| `web_fetch` url | 页面HTML内容 | ~Nk |
| … | … | ~Nk |
| **工具结果小计** | | **~Nk** |

### 输出侧

| 环节 | token | 具体内容 |
|------|------:|---------|
| 深度思维链 (`thinking`) | ~Nk | ① 判断… ② 规划… ③ 推导… |
| 文本回复 (`text_output`) | ~Nk | ① 结构分析 ② 速查表 ③ 注意事项 |
| 工具调用 (`tool_calls`) | ~Nk | ① read_file x2 ② search_content x3 ③ … |
| **输出小计** | **~Nk** | |

### 汇总

| 指标 | token |
|------|------:|
| **本轮合计** | **~Nk** |
| **会话累计** | **~Nk** |

### 消耗热点分析

| 排名 | 热点 | 占比 | 优化建议 |
|------|------|------|---------|
| 1 | … | N% | … |
| 2 | … | N% | … |
```

## 估算规则

1. **单位**：以 **k**（千）为单位，保留 1 位小数。如 1500 token → `1.5k`，200 token → `0.2k`
2. **中文字符**：1 个汉字 ≈ 1.5-2 token（取决于分词器）
3. **英文字符**：1 个英文单词 ≈ 1-1.5 token
4. **代码**：按字符数 / 3 估算（代码中符号和空格较多，token 密度较低）
5. **工具结果**：按实际返回内容长度估算，工具调用的元数据开销（函数名、参数结构）约 20-50 token/次
6. **系统提示词**：首次出现时标注估算值，后续轮次若无变化可标注 `(不变)`
7. **会话累计**：从对话开始到当前轮次的累计总量

## 具体内容列填写规范

- **系统提示词**：列出主要构成部分（角色定义、工具schema、content_policy、规则等）
- **用户规则**：逐条列出加载的规则名称及估算量
- **记忆/上下文**：逐条列出注入的 memory 标题及估算量
- **对话历史**：说明之前轮次数和大致内容类型
- **工具结果**：拆分为独立子表，每行一个工具调用，标注返回内容摘要
- **深度思维链**：列出推理步骤（①②③…）
- **文本回复**：列出回复的主要章节/段落
- **工具调用**：列出调用的工具类型和次数

## 简化规则

- 对于 token 消耗极少的环节（< 0.05k），可合并为 `其他` 行
- 若本轮无工具调用，输出侧的 `tool_calls` 行标注 `0`
- 若本轮无图片/附件，输入侧的 `attachments` 行标注 `0`
- 工具结果拆解表：若本轮无工具调用，可省略整个拆解表
- 消耗热点分析：至少列出前 3 个热点，占比按本轮合计计算

## 消耗热点分析规范

- **占比**：按本轮合计 token 数计算百分比
- **优化建议**：针对该热点给出具体可操作的优化方案（如"先 search_file 再 read_file"、"用 grep 提取 template 部分"等）
- **路径试错**：若存在多次路径试错导致重复读取，应单独列出并在优化建议中说明