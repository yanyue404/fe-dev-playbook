---
description: 
alwaysApply: false
enabled: true
updatedAt: 2026-08-03T03:06:25.185Z
provider: 
---

# Mermaid Flowchart 语法校验规则

> 本规则基于实际踩坑经验总结，确保生成的 Mermaid flowchart 在所有渲染器（包括 yitools.top/mermaid-editor）中不报错。

---

## 1. 核心原则：特殊字符必须用双引号包裹

**Mermaid 节点标签中只要包含以下任意特殊字符，就必须用 `["..."]` 双引号包裹整个标签文本：**

| 特殊字符 | 被解析为 | 示例 | 修复 |
|----------|---------|------|------|
| `=` | PS token（等号语法） | `D1[=SUM(B2:B9)]` | `D1["=SUM(B2:B9)"]` |
| `:` | SQE/属性分隔符 | `D2[专业字体: Arial]` | `D2["专业字体 Arial"]` |
| `#` | TAGSTART（HTML标签开始） | `F2[#REF! 错误]` | `F2["#REF! 错误"]` |
| `!` | 标记符号 | `注意!` | `注意!`（在双引号内安全） |
| `{}` | 菱形决策节点语法 | `输出 {name}.zip` | `输出 name.zip`（去掉花括号） |
| `<>` | HTML 标签 | `< 5k 词` | `"< 5k 词"`（双引号包裹） |
| `()` | 子程序节点语法 | `func(param)` | `"func(param)"` |
| `[]` | 方括号节点语法 | `[output-dir]` | `"output-dir"`（去掉内层方括号） |

### 快速判断规则

```
节点标签文本中 是否含 = : # ! { } < > ( ) [ ] 中的任意一个？
  ├── 否 → 安全，可直接写 A[普通文本]
  └── 是 → 必须用双引号包裹 A["含特殊字符的文本"]
```

---

## 2. 中文引号绝对禁止

**中文引号在任何位置都会导致 Mermaid 解析失败，必须彻底移除或替换：**

| 禁止使用 | 替代方案 |
|----------|---------|
| `"` `"`（中文双引号） | 用空格分隔，或用英文双引号在 `["..."]` 内 |
| `'` `'`（中文单引号） | 直接去掉，或用英文引号替代 |

**示例：**

```
❌ S1c[避免模糊需求<br/>如"帮我做数据分析"]
✅ S1c["避免模糊需求<br/>如 帮我做数据分析"]
```

---

## 3. 占位符花括号规则

**花括号 `{...}` 在 Mermaid 中是菱形决策节点语法，禁止用作占位符：**

```
❌ S5e[打包为 {name}.zip]        → 被解析为菱形节点
❌ B1[name: {skill-name}]        → 被解析为菱形节点
✅ S5e["打包为 name.zip"]        → 直接写具体值
✅ B1["name: skill-name"]        → 去掉花括号
```

> **注意**：菱形决策节点 `{是/否}` 是合法 Mermaid 语法，不要误删。

---

## 4. 换行 `<br/>` 使用规则

- `<br/>` 在双引号节点内可正常使用：`A["第一行<br/>第二行"]`
- `<br/>` 在无特殊字符的裸节点内也可使用：`A[第一行<br/>第二行]`
- **但一旦节点内有其他特殊字符，就必须同时加双引号**

---

## 5. emoji 使用规则

| 规则 | 说明 |
|------|------|
| 禁止残缺 emoji | 变化选择符 `️` 必须跟在基础 emoji 后，不能单独出现 |
| emoji 在裸节点安全 | `✅ 完成` 在无其他特殊字符时安全 |
| emoji + 特殊字符需双引号 | `⚠️ data_only=True` → `["⚠️ data_only=True"]` |

---

## 6. 连接线标签规则

连接线上的标签 `-->|标签|` 也有类似限制：

```
✅ B -->|读取分析| C           → 简短中文安全
✅ B -->|是| F3                → 单字安全
❌ B -->|Step 1: 解包| C       → 含冒号，需改为 -->|"Step 1 解包"| C
```

---

## 7. 节点 ID 命名规则

节点 ID（如 `A`、`B1`、`S5c`）必须满足：

| 规则 | 说明 |
|------|------|
| 字母开头 | `A`、`Step1`、`S5c` ✅；`1A` ❌ |
| 仅含字母数字 | `F3` ✅；`F-3` ❌（连字符在某些渲染器有问题） |
| 不与 Mermaid 关键字冲突 | `end`、`subgraph` 等是关键字，避免用作 ID |

---

## 8. 生成前自检清单

每次生成 Mermaid flowchart 后，逐项检查：

```
□ 1. 所有含 = : # ! { } < > ( ) 的节点标签是否已用 ["..."] 包裹？
□ 2. 是否存在中文引号 "" ''？如有，是否已替换/移除？
□ 3. 是否存在花括号占位符 {name}？如有，是否已去掉花括号？
□ 4. 连接线标签是否不含 : = 等特殊字符？如有，是否用双引号？
□ 5. emoji 是否完整（无残缺变化选择符）？
□ 6. 节点 ID 是否纯字母数字、字母开头、无关键字冲突？
□ 7. <br/> 是否只在安全上下文中使用？
```

---

## 9. 安全 vs 危险 对照速查

### ✅ 安全写法

```mermaid
flowchart TD
    A[普通文本] --> B{决策节点}
    B -->|是| C["含等号 =SUM 公式"]
    B -->|否| D["含冒号 Step 1 说明"]
    E --> F["含井号 #REF 错误类型"]
    G --> H["含尖括号 < 5k 词限制"]
```

### ❌ 危险写法（会导致 Parse Error）

```mermaid
flowchart TD
    A[=SUM公式] --> B        ← 等号裸露 → PS token 报错
    B --> C[Step 1: 说明]     ← 冒号裸露 → SQE 报错
    D[#REF错误] --> E        ← 井号裸露 → TAGSTART 报错
    F[< 5k词] --> G          ← 尖括号裸露 → 解析异常
    H[如"数据分析"] --> I     ← 中文引号 → STR 报错
    J[输出{name}.zip] --> K   ← 花括号 → DIAMOND_START 报错
```

---

## 10. 一句话总结

> **节点标签含特殊字符 → 加双引号；中文引号和花括号占位符 → 永远禁止。**